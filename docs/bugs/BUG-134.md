# BUG-134：连接态三处重复渲染 + 速度双实现（一处绕过 selectors）+ 灯尺寸不齐

- 状态: fixed
- 发现日期: 2026-09-22
- 模块: shell
- 严重程度: mid

## 现象

用户原话（2026-09-22）：「实时已连接,存在3个状态显示,右上,左下,右下。」

- 右上/左下/右下三个「已连接」灯同屏，用户不知该信哪个；**左下的灯明显更大**，看起来像不同含义却写着同一文案。
- 第二重冗余（用户未提及，同病症）：下载速度也是**两处显示**（上下文栏 chip + 底部状态栏），且两处实现不同源。

## 根因

**三处已同源**（BUG-087 已修，全部读 `store/connState.ts` 的 `useConnState()` + `CONN_LABEL` + `CONN_DOT`）——问题性质是**同一信息重复渲染**，不是数据打架：

| 位置 | 锚点 | 灯尺寸 |
|---|---|---|
| 右上 上下文栏 | `ContextBarStatus.tsx:76-81`（`data-testid="context-conn"`） | `h-1.5 w-1.5` |
| 左下 侧栏底部 | `Sidebar.tsx:351-358`（`data-testid="sidebar-conn"`） | **`h-2 w-2`** ← 唯一不一致 |
| 右下 底部状态栏 | `MainLayout.tsx:309-317`（`data-testid="statusbar-conn"`） | `h-1.5 w-1.5` |

速度双实现：

- `ContextBarStatus.tsx:28-42` `SpeedChip` → 订阅 `selectors.selectActiveSpeedLabel`（等值短路，符合 AGENTS.md §5）。
- `MainLayout.tsx:129` `totalSpeed` → **直接 `downloads.filter(...).reduce(...)` 绕过 selectors**（`:316-318` 渲染）。文件注释自承认「口径一致但不是同一个数据源」——漂移隐患。

## 复现

任意页面同屏三处灯，比对 DOM class；断线时三处同步变化（同源），但用户无法判断哪处是权威。

## 修复

方案待用户拍板（评估文档 Q1）：

- **方案 A（推荐）**：收敛为两处差异化语义（侧栏=进程态 / 上下文栏=链路态），去掉右下；速度合并为一处；灯尺寸统一 token。
- **方案 B**：保留三处，强制差异化语义 + 统一尺寸。

无论 A/B：`MainLayout` 若保留速度，必须复用同一 selector（注意它当前不订阅 `downloads`，改订阅需 memo 隔离，遵守 AGENTS.md §5 流畅优先）。三处锚点（`data-testid` + `data-conn-state`）为现成验收通道。

## 关联

- BUG-087（同源化先例）、BUG-083（上下文栏）
- `docs/design/tg-media-ux-confusion-assessment-2026-09-22.md`（S1-S3、Q1）
- handover 报告 §5.1 A1

## 验收证据

**修复提交**：`f437571`（fix(shell): unify conn-light token and route status-bar speed through selector）

**已执行验证**：
1. `npm run build`（tauri-shell）→ `tsc && vite build` 通过，1963 模块，0 类型错误。
2. 代码核对（满足原判据）：
   - 判据 2（灯尺寸一致）：`Sidebar.tsx:355` 的 `h-2 w-2` 已改为 `h-1.5 w-1.5`，
     与 `context-conn` / `statusbar-conn` 一致（三处现同为 `h-1.5 w-1.5`）。
   - 判据 3（速度复用同一 selector）：`MainLayout.tsx` 删除自算 `active`/`totalSpeed`
     （原 `:112`/`:129`），改为 `useStore(selectActiveSpeedLabel)` + `useStore(selectHasActiveSpeed)`
     （`:34-35`），与 `ContextBarStatus` 的 `SpeedChip` 同源（`store/selectors.ts`）。
     `formatSpeed` 导入已移除，无第二套速度口径。
   - 判据 1（三处同步）：自 BUG-087 已同源（`useConnState()` + `CONN_LABEL` + `CONN_DOT`），
     本次未改动该路径；`data-testid` 锚点（`context-conn` / `sidebar-conn` / `statusbar-conn`）保留。

**反向证伪（环境限制说明）**：判据 1 要求「断线 → 恢复时三处同步变化」的**运行期** DOM 断言，
本环境为 Windows + WebView2（agent-browser 不支持 Windows）无法跑无头浏览器实采，
故未执行运行期反向证伪。该路径自 BUG-087 已同源，本次仅消除尺寸/双实现漂移；
如需运行期验收，待 Linux/macOS 或本机 WebView2 下用 playwright / `diag_*` 跑断线用例。

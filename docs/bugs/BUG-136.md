# BUG-136：cache 接口 external 字段双形状双口径——stats 返数字、clear/preview 返对象

- 状态: open
- 发现日期: 2026-09-22
- 模块: engine
- 严重程度: low

## 现象

同一个 `external` 语义（「下载目录之外的已登记文件」），两个接口返回**两种 JSON 形状**，且**口径不同**：

| 接口 | 形状 | 口径 |
|---|---|---|
| `GET /api/cache/stats` | **数字** | 只计数、**不累加字节**（命中外部文件直接 `continue`） |
| `GET /api/cache/clear/preview` | **对象**（含 `bytes`、`removable: false`） | 计数 **且累加字节** |

前端若想展示「外部文件占了多少」，只能依赖 preview 那一次；stats 的 external 答不出字节。

## 根因

- `cache.rs:858-870`（stats）：`if !inside_dir(&dir, &it.file_path) { external += 1; continue; }` —— 一个字节都不加。
- `cache.rs:611-616`（clear_preview）：`else { external += 1; external_bytes += size; }` —— 累加。

## 复现

对同一份含外部文件的库分别请求两接口，比对 `external` 的类型与 bytes 口径。

## 修复

归一为同形状（建议对象 `{count, bytes}`）并统一口径（外部文件字节是否计入要有唯一答案）；若兼容成本高，至少在两处响应注释与前端消费处写明差异。验收注意本机 curl 坑：加 `--noproxy '*'`、判成败只看 `-w '%{http_code}'`（`-o /dev/null` 恒 exit 23）。

## 关联

- BUG-109（响应如实性）
- `docs/design/tg-media-ux-confusion-assessment-2026-09-22.md`（S12）
- handover 报告 §5.2 B4

## 验收证据

> **当前状态 `open`，尚未修复。** 以下不是已执行的验收记录，而是**修复后必须满足的可证伪判据**
> （预先声明，防止修完再编造判据）。修复提交时必须把本节替换为实际执行记录（命令 + 输出）。

**判据（每条都必须能 FAIL）**：

1. `GET /api/cache/stats` 与 `GET /api/cache/clear/preview` 响应中的 `external`
   必须是**同一种形状**（当前前者为数字、后者为对象）。
2. 两个端点对同一批数据的 `external` 计数必须相等。

**反向证伪**：先造一个「下载目录外文件」用例，断言两个端点都把同一文件计入 external；
若任一端恒返回 0，断言必须 FAIL。

**本机注意**：`curl -o /dev/null` 在本机恒返回 exit 23，判成败只看 `-w '%{http_code}'`，且须加 `--noproxy '*'`。

**取证位置**：`download-engine/crates/orig-tg/src/cache.rs`（`stats` 与 `clear_preview`）。

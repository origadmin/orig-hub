# ============ Rust（下载内核 + Tauri 壳） ============
/target
**/target
# mock 构建用的独立 target 目录（CARGO_TARGET_DIR=target-mock）；`**/target` 匹配不到它
**/target-mock/
**/*.rs.bk

# ============ Node / 前端（Vite + React） ============
node_modules/
dist/
dist-ssr/
.vite/
*.local

# ============ Tauri 构建产物 & sidecar 二进制 ============
tauri-shell/src-tauri/target/
tauri-shell/src-tauri/binaries/
*.exe
*.dll
*.so
*.dylib

# ============ 运行时产物 ============
/bin/
/data/
*.orighub
*.db
proxy-test-file.bin
coverage/
verify_shots/
docs/preview/
download-engine/acceptance-evidence.txt

# ============ 日志 ============
*.log
logs/

# ============ AI/Agent 工具目录（本地专属，绝不入库 · 零容忍） ============
# 整块照抄 orig-cms-ee 根 .gitignore（其 AI/Agent 工具清单）。
# orig-hub 此前缺此整块，导致 .trae 被误提交；现对齐 EE，禁止任何 AI 临时/配置目录入库。
.claude/
.trae/
.workbuddy/
.agents/
.agent/
.cursor/
.coder/
.augment/
.sourcegraph/
.continue/
.aider/
.aider*
.kiro/
.cody/
.codex/
.codeium/
.windsurfrules
.cursorignore
.cursorrules
CLAUDE.md
.aider.conf*
skills-lock.json
.github/copilot/

# ============ 工具元数据（不入库） ============
.beads/
.task/
.code-review-graph/
.team/
__pycache__/

# ============ 编辑器 / OS ============
.vscode/*
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

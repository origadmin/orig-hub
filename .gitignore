# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go
go.work
go.work.sum
*.test
*.out
*.prof
coverage.out
coverage.html
coverage

# Frontend build output
ui/dist/

# Frontend dependencies
ui/node_modules/

# Wails generated bindings
ui/wailsjs/

# Bun
ui/bun.lock

# npm (project uses bun)
ui/package-lock.json
ui/package.json.md5

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Data (runtime generated)
data/
*.db
*.db-shm
*.db-wal

# Build artifacts
build/windows/installer/project.nsi
build/windows/wails.exe.manifest

# Docs (internal, not for repo)
_docs/
docs/

# Internal design briefs (not for repo)
STITCH_DESIGN_BRIEF.md

# Temporary test / scratch directories
tmp*/
_tmp*/
_gitstatus.txt

# Backup (deleted-but-kept) directories from team-flow
_back/

# team-flow v3 runtime (per-flow state, not for repo)
.team/state/
.team/sessions/
.team/.flow_update_cache.json
.team/state.yaml
.team/constraints.md
.team/project.yaml
.team/skills-resolved.yaml
.team/flows/

# Beads runtime (issue tracker local state, NEVER commit)
.beads/

# Task (Wails build cache)
.task/

# Wails generated frontend bindings (regenerated by `wails generate`)
ui/bindings/

# Task runner alternative (project uses Makefile only)
Taskfile.yml


# Misc
*.log
.env
.env.*

# Beads / Dolt files (added by bd init)
.dolt/
.beads-credential-key

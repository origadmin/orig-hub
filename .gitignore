# ============ Rust（下载内核 + Tauri 壳） ============
/target
**/target
**/*.rs.bk

# ============ Node / 前端（Vite + React） ============
node_modules/
dist/
dist-ssr/
.vite/
*.local

# ============ Tauri 构建产物 & sidecar 二进制 ============
tauri-shell/src-tauri/target/
tauri-shell/src-tauri/binaries/
*.exe
*.dll
*.so
*.dylib

# ============ 运行时产物 ============
/bin/
/data/
*.orighub
*.db
proxy-test-file.bin
coverage/
verify_shots/
docs/preview/
docs/prototypes/
download-engine/acceptance-evidence.txt

# ============ 日志 ============
*.log
logs/

# ============ 工具元数据（不入库） ============
.beads/
.task/
.code-review-graph/
.team/
.workbuddy/
__pycache__/

# ============ AI/Agent 工具目录（ZERO TOLERANCE，绝不入库） ============
# 对齐 EE：AI 工具运行态/配置一律本地化，提交即规范事故（AGENTS.md §6.3）
.claude/
.trae/
.agents/
.agent/
.cursor/
.coder/
.augment/
.sourcegraph/
.continue/
.aider/
.aider*
.kiro/
.cody/
.codex/
.codeium/
.windsurfrules
.cursorignore
.cursorrules
CLAUDE.md
skills-lock.json
.github/copilot/

# ============ 仓库根唯一临时出口（AGENTS.md §6.1） ============
# 临时/处理/草稿产物只许落这里；根目录与工作目录严禁落临时文件
/.scratch/

# ============ 构建/临时残留 ============
# mock 构建目标、git 操作临时对象目录、通用临时文件
**/target-mock/
.git-tmp-obj/
*.tmp
*.bak
*.orig

# ============ AI 生成的报告/摘要（不入库，AGENTS.md §6.2） ============
REPORT.md
FINDINGS.md
TRIAGE.md
RCA.md
FIX.md
AC.md
IMPL.md
SCOPE.md
SPEC.md
analysis*.js
analyze*.js

# ============ 编辑器 / OS ============
**/.vscode/
**/.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ============ Cargo 本地环境配置（不入库） ============
# 含本机网络镜像源(rsproxy 等)或代理，属本地环境，换机/CI 不应继承
**/.cargo/config.toml
**/.cargo/config

# ============ 本地验证脚本（非交付代码） ============
# e2e/verify 类脚本按用途归类为本地验证，不入库（对齐 EE 的 probe_*.mjs 规则）
tauri-shell/verify-e2e.mjs
.workbuddy-ai/
